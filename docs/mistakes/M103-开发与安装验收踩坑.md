# M103：开发与安装验收踩坑

> 分类文件：两套 BH_HOME 混用导致的验收假象。行级条目接编在下方；新坑当场落行，二犯升格配机器约束。

| 行级编号 | 坑 | 处置 | 状态 |
| --- | --- | --- | --- |
| M014 | 开发态与安装态各有一个家目录，混用后看板「应用监控信息不刷新」。仓库里跑 `node dist/dashboard.js` 时 `isDevCheckout()` 为真，BH_HOME=`<repo>/.bh-dev`；已经在跑的 page-detect / supervisor-core 是安装态 `bh` 拉起的，status 写在 `~/.config/browser-harness/data`。看板读空目录，卡片恒「未运行」。`npm install -g .` 与 `npm link` 也会被判成开发态（包根仍能看到 tsconfig.json） | 两套家禁止混用。开发：`node dist/cli.js` 或 `npm link`，数据只在 `.bh-dev`。给人看的安装验收：`npm pack` 再 `npm install -g ./browser-harness-ts-*.tgz`，只用 PATH 上的 `bh`，数据只在 `~/.config/browser-harness`。不要从仓库起 dashboard 去看安装态正在跑的应用。流程权威：R001「开发 vs 安装验收」；协作禁令：AGENTS 操作节 [实证: 2026-09-07 看板 dataDir=.bh-dev 而 page-detect.status.json 写在用户目录，16s 后对齐用户 BH_HOME 才见 ts 变化] | 已机制化（文档约束） |

## 复发监控

- 再出现「看板应用卡不刷新 / 未运行但 rmux 里进程活着」：先对 `bh doctor` 与看板左栏「数据目录」是否同一 BH_HOME，不要先改应用
- 二犯：给 `ensureDashboard` / `dashboard start` 打 BH_HOME 来源断言（开发态拒绝读默认用户目录，或启动时打印 homeDir）
