# 2026-09-07：D12 看板 + D13 原语结构重组 + 0.2.0 封版本地安装

## 主线

1. **D12 看板**：bh dashboard（127.0.0.1:9870，SSE 2 秒）三栏：左 = 安装与资源（折叠组：安装/目录/应用/SKILL 资产），中 = 工作实例/浏览器状态/后台任务/事件四区全列名表格化，右 = 日志消息（累加置顶滑入 + 状态卡心跳/节奏/库存/时间线直读 sqlite）。macOS/iOS 视觉全对齐（系统色/圆点状态/SF 度量/像素对齐），20+ 轮用户打磨定稿。
2. **D13 结构重组**：四层原语栈（primitives/interaction-skills/domain-skills/apps）；skill/primitives/ 五文件权威源；workspace 数据全迁 BH_HOME/data（结构/数据分离）；漂移清零（97->94 是空壳目录）；x 系六应用合一为 x-intel（入口+组件目录+角色参数启动+自执行守卫+selfManaged 不抢 default daemon）；应用改名对齐（cookie-io/page-detect）。
3. **附着策略修正**：default 空白优先于马标记（绝不偷应用守护进程的工作 tab）；触发节奏改 5 秒探测+命中 10 秒内随机延时。
4. **0.2.0 封版本地安装**：npm pack + 全局拷贝式安装（脱离 link 与 .bh-dev），用户目录 ~/.config/browser-harness 就位（应用/数据/注册表），全链从用户目录实跑验证（x-intel@9958 收割 604 帖）。
5. **D14 vendor 豁免**：evo 重检文档体系（check PE-01~13 全绿；scan 3730 条告警全落上游移植资产与 .bh-dev 运行区，正式文档零命中）；裁定不回改上游正文，PRD 登记 D14 + G001 落 vendor 资产禁字豁免条款（assets/domain-skills、skill/interaction-skills、protocol/*.json）。
6. **D15 medium-search 应用**：medium 站内搜索 + 文章抓取落成标准插件（SDK `__ms` 1.0.0 + apps/medium-search.mjs，两步契约 ms_search/ms_article，人机共存 tab 策略：仅复用当前 medium tab 否则新开）；G002 六门禁全过，墙门禁被真墙实证（CF「请稍候」页如实报 exit 2 不硬闯）。关键实测：冷 HTTP format=json 403、页内 fetch 连续请求被 CF tarpit、过墙后搜索会话限流（M011/M012）。
7. **D16 看板墙提醒（第一期）**：Chrome Notification 实证研究（127.0.0.1 secure context、一次授权常驻、requireInteraction 驻留、tag 去重）后落地：verdict 边沿进入墙类三判弹系统通知 + 右栏钉住横幅 + 权限引导按钮；非实例残留条目剪枝。验收：hook 真通知弹出、横幅渲染与剪枝、按钮态正确。
8. **issue #1 修复（tab 所有权两处断裂）**：外部用户报告四项全核实。修复 A：createTarget 注册钩子从 Harness.dispatchRaw 下沉 Session._call（onCreateTarget 回调，与 callGuard 同层对称，--new-tab 旁路路径不再漏注册）；修复 B：attachFirstPage 前置 enableTargetDiscovery（setDiscoverTargets，connect 重臂），targetInfoChanged 复活。实测：--new-tab 建 tab 后 close_tab 成功（5->4）；事件环见 targetInfoChanged。回归测试 56/56。附注 libuv exit 9 本机 Node 24.20 未能复现，待报告者补充。
9. **D17 应用卡片区**：终版布局为**右侧两个独立区**（上「应用」七卡：description 常量 + 运行流水 + stderr 日志行 + cache 库存 + 墙类高亮、可折叠拖拽；下原「日志消息」区原样保留，横幅标题改 tag 与名字齐平）；平台层 runPlugin 记 app-runs.jsonl（2MB 轮转，应用零改造）；部署信息栏默认隐藏；主列仅系统卡。
10. **D18 page-detect 守护监测**：watch/unwatch/status 常驻只读探测全部 http(s) 页面；墙类边沿告警（Google /sorry·reCAPTCHA、Cloudflare 挑战、登录墙、资源被 CF 阻断 performance 403/429/503、白屏两周期持久化）落 data/page-watch.json；看板快照并入，通知吃 alerts 流（首帧基线防重放），右栏墙横幅同步。注入验证：告警 -> 系统通知 + 横幅钉住 -> 清除退场。
11. **D19 初始化原语**：Node ≥22 版本闸；ensureDaemon 成功或半活皆 fire-and-forget 带起看板（ensureDashboard 幂等）与 rmux 守护（startServer）；不动既有附着。看板页加版本握手（pageV djb2）自动重载，破「SSE 只推数据不换代码」的旧页滞留问题。实测：杀看板 -> daemon 重生 -> 9870 自动 200 回归。

## 踩坑（当日连环）

- rmux 会话死但 pane 壳残留骗过 hasSession；杀进程须走会话级
- 插件入口直跑无人调 main：自执行守卫须按 process.argv[1] 判定；角色派发后不可 return（会触发 process.exit 杀常驻循环）
- runPlugin 无条件 ensureDaemon(default) + 马标记优先 = default 抢应用 tab：selfManaged 声明 + 空白优先双修
- 模板字面量里的正则反斜杠被吃（页面脚本语法错误）;多行模板里 TS 注解泄漏
- worker 触发器 drain_events 清空事件环饿死看板 peek：触发只探测不清空
- repl 多语句片段不自动 return（静默空输出 exit 0）；模板内嵌 \n/三反引号被外层先解释 + js() 吞页内异常 = 「空输出」三重假象（M009/M010，全天最大时间黑洞）
- CF 对页内连续 format=json fetch tarpit（首 2 次 200 后挂起）；Medium 过墙后搜索会话限流而文章页正常（M011/M012）
- 开发态看板（.bh-dev）去读安装态应用 status（~/.config/browser-harness/data）：应用监控台恒空/不刷新（M014）
- 用户 pwsh 有 ffmpeg 9.0.1（D:\ohmyenv\ffmpeg\bin），agent/`bh doctor` 的 PATH 没有：改为合并 User+Machine PATH；ffmpeg 9 拒 `-vsync`，改 `-fps_mode vfr`。mp4 冒烟 1655B（M005）

## D22/D23/D24（搜索标准化与极限测试）

- D22 bing-search G002：`__bs` 1.0.0 + `bs_search` 两步契约。实搜「TypeScript browser」`--top 5` 指标 count=5 bytes=1290；pluck 五条 title 均非空（旧版 GitHub 行 title 空已消）。无挑战页。[实证: 2026-09-07]
- D23 x-intel search/harvest：`--stats` `{_ok,_v,_ts,total_tweets:604}`；`search TypeScript --limit 3` 出 items 数组。harvest 完成改为指标 JSON（本轮未跑分片收割）。
- D24 极限测试（开发态 `.bh-dev`）：google「TypeScript 稳定操作浏览器」5 条；bing 同上；web-fetch Chrome 博文 word_count=593 engine=browser+browser-retry；kentl.dev DNS 失败如实返回 chrome-error 页（word_count=55），不伪装空成功。无 CAPTCHA 本轮。

## D25 super-ocr

- 新应用：扫描当前（或 url 片段匹配）页，`__ocr.locate` 定位验证码图（img/canvas/svg/bg，关键词+邻近标签+尺寸），导出海报或 CDP clip，ppu-paddle-ocr 识别。不自动填写。
- 引擎不进核心 `package.json`：`bh super-ocr setup` 装到 `<BH_HOME>/ocr`。交互式拼图/滑块走 CAPTCHA|WALL。
- 踩坑：M016 fnm 下 `spawnSync('npm.cmd')` status null，改走 `node npm-cli.js`；M015 Windows `D:\` 路径被 ppu-paddle-ocr 当 Canvas，改传 ArrayBuffer。
- 安装验收（R001，与开发态分开）：`npm pack` + `npm install -g tgz`，`bh --version` 0.2.1 npm-global、无 tsconfig；`bh skill sync` 铺 `apps/super-ocr.mjs` + `sdk/ocr.min.js`；`bh super-ocr setup` 装到 `C:\Users\ray\.config\browser-harness\ocr`；全局 `bh super-ocr` 识别 fixture **K8M2** conf=0.999。doctor 全绿（含 ffmpeg PATH）。看板 9870 已拉起。

## GitHub issues #1/#2

- #1 代码已在 f7eb4ed（createTarget 下沉 Session._call + setDiscoverTargets）。补 attachFirstPage 扫描马标记 tab 写入 ownedTargets，覆盖 daemon 重启后 close_tab 失联。
- #2 `fill_input` 回读 + 激活重试；`timeoutSeconds` >3600 告警封顶 600s；`/eval?timeout=` 与 CLI `BH_EVAL_TIMEOUT` 默认 300s，超时文案指向 `bh --restart`。

## supervisor-core 通用守护

- 删 `x-intel.mjs` 的 `supervisor` 角色与 `x-intel/supervisor.mjs`。`bh x-intel start` 只拉 rmux `x-monitor` worker；`stop` 先写 stopped 再杀 worker + 专属 daemon，并清掉遗留 `x-supervisor`。
- `supervisor-core` 监督登记：page-detect（常驻）+ x-intel（onDemand，status=running 才拉起）。不再双循环。

## D11 触发版复验

- 安装态 `bh x-intel start`：rmux `x-supervisor` + `x-monitor` 拉起；用户 Chrome 已开 `https://x.com/home` 标题 `(1) 主页 / X`。
- 21:51:48 启动；21:52:02 首次刷新（约 14s = 5s 探测 + 10s 内随机抖动，**不是** 300s 兜底）：页面检测到 15 帖，新入库 9，库存 1003 增至 1012。随后两拍 11 帖全是已存在（去重）。`seen_range` 最新 `2026-09-07 13:52:02`。[实证: x_worker.log + `bh x-intel search --stats` total_tweets=1012]

## 第三方 review 修复闭环（grok，6f1bf28..ca23f93，11 条全修）

- 6 bug：白屏持久化告警改阈值跨越（M017）；x-intel start 冷启动补拉 supervisor-core（selfManaged 此前无人守护 worker）；/eval 单飞互斥（超时未结算期间新求值 429）；Target 脱离追踪改 sessionId（M018，params.targetId 系可选废弃字段，缺省时附着状态滞留）；page-detect interval 持久化（companions 与守护重拉沿用，watch 请求不同节奏杀会话重拉）；看板 URL 判定改 URL 解析精确匹配（98700 前缀同形端口不再误判，page-detect/super-orchestra 复用 dist host.ts 共享判定）。
- 建议 7-11：fill_input 表单控件回读严格相等（预含针值不再误判成功）；setDiscoverTargets 失败不闩死本连接发现；super-ocr NOT_FOUND 退码 3 对齐 G002；cdp() JSDoc 归位；看板 1s 推送注释对齐。
- 测试 74/74（新增 host.test.ts 4 组、fill_input 包含针值 1 例、detach sessionId 2 例）。
- 安装态验收五项冒烟全过：白屏 3 周期恰 1 条告警；杀 supervisor-core 后 x-intel stop/start 守护全回；eval 504 后重试 429、结算自动放行；interval 5 到 30 重拉生效（已恢复 10）；doctor 全绿。
- README 重整：内置应用一览表（命令形状对源码 USAGE 核准）+ 环境变量配置表（默认值从源码核准）；修落单代码围栏（其后全文被吞进代码块）与「没有 helpers 文件」过时表述。
- 踩坑补记：直接调 rmux CLI 命中别的 pipe（哈希随环境变），须借 daemon 侧 Rmux 类杀会话。

## 提交

- fix: 第三方 review 修复（6 bug 与建议 7-11）
- docs: README 部署配置与应用一览重整
- fix: supervisor-core 看板卡片指标改为守护进程
- feat: supervisor-core 统一守护 page-detect 与 x-intel
- fix: GitHub issues #1/#2 所有权认领、fill_input 回读、eval 超时
- docs: D11 触发版复验闭环（x.com 标题徽章 14s 内 +9 入库）
- fix: Windows 用户 PATH 探测 ffmpeg，适配 ffmpeg 9 fps_mode
- feat: super-ocr 扫描定位验证码图并用 PaddleOCR 识别
- docs: D14 vendor 资产禁字豁免条款（PRD 登记 + G001 落条款）