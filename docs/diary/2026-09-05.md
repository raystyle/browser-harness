# 2026-09-05：D11 架构转向（附着用户浏览器 + 人机共存）

## 今日主线

一天走完一个完整需求的五步（登记 -> 立项 -> 执行 -> 验收进行中 -> 待归档）：

1. **立项拷问（3 轮）**：用户指令「重构程序」起手，动机拷问揭出真实取向「只附着用户打开的浏览器、人机共存、不影响人类浏览」。裁定：架构转向需求（忠实移植原则在本需求让位）、spawn 家族彻底移除、专属 tab 铁律 + 显式授权、用户自开为主、研究证伪即告警退出（无兜底）。共识落 PRD D11。
2. **S001 生死门研究**：Chrome 136 禁令属实（官方博客原文取回），但 Chrome 144+ 官方 auto-connect 通道与 D11 完全同构；本机 Chrome Dev 155 PoC 五步全绿（DevToolsActivePort 发现 -> WS 直连 -> 列 tabs -> 后台建 tab -> 清理），用户肉眼确认不抢焦点、每条新连接弹一次 Allow。判定可行，discovery 设计输入成文。
3. **测试网先行**：48 用例锁存活语义（env setdefault 链、BH_HOME/实例注册表、CDP 路由与发现排序、键位物理真值、tab 纪律、环形缓冲、自愈链）。先绿后动，全程没破过。
4. **架构落地**：session.ts 候选表补 Chrome Dev/Beta（本机 Chrome Dev 此前扫不到）、排除 Edge（用户指令）；harness connect 三级入口改发现附着 + 空态指引；attachFirstPage 统一专属 tab 铁律；idle 退出只关自身连接；删 agentChrome/taskIsolation/locks 三文件与 bh chrome/chrome-mode/--once/--batch 命令面；x-monitor 去 spawn 依赖加附着预检。
5. **真机冒烟**：doctor「browser attachable」直指 Chrome Dev @ 9222；daemon 附着成功，专属 tab 落 about:blank 带马标记，未触碰用户正开的 Google/Cloudflare 页；sessions 全景新口径正确。
6. **文档同步**：README/SKILL/R001/CHANGELOG 基线换附着模型；SKILL 新增「附着模型（D11 起：人机共存）」节。

## 待收口

- x-monitor 实跑冒烟（需用户在场点 Allow）：起监控 -> 专属 tab 工作 -> 用户关浏览器 -> supervisor 只重拉 worker
- toggle 重启持久性观察（S001 遗留项）
- 验收全绿后：PRD D11 置已交付、GOAL 历史回填、P 方案归档

## 心得

- 「重构」一词的追问链价值：三轮拷问把一个模糊词收敛成一次有生死门的架构转向，避免了照字面做纯结构重构的完全跑偏。
- 生态勘景先行：Chrome 官方自己把「agent 连个人浏览器」做成了正式功能（auto-connect），D11 与平台方向同轨，这是研究先行最大的红利。
- 测试网锁的是「存活语义」而不是「现状」：专属 tab 铁律的新行为测试与实现同批落，先写现状测试再改结构的纪律没有和架构转向冲突。

## 提交

（待用户指示后提交：feat 前缀，一事一提交）
